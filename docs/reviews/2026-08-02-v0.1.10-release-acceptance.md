# v0.1.10 发布与正式安装包验收

- 日期：2026-08-02
- main 发布提交：`72b05c2e9ee053eb194ff8320aefe8af009bcec9`
- annotated tag：`v0.1.10`
- Release：[v0.1.10](https://github.com/dff652/ai-asset-hub/releases/tag/v0.1.10)
- main 候选 PR：[PR #47](https://github.com/dff652/ai-asset-hub/pull/47)
- 结论：**main/tag 来源、线上资产、v0.1.9 → v0.1.10 严格升级、安装包 CLI readiness
  三状态、TUI 首页「换机与备份」入口、MCP 8 工具与零写入均通过；可把 installer
  默认 pin 与用户文档收口到 v0.1.10。**

## 1. main、tag 与 CI

- 候选 head `6fae3fa` push/PR CI 全绿后 squash 合入 `main@72b05c2`
- main CI run `30708022600` 在 `headSha=72b05c2e9ee0…` 上 success
- `v0.1.10^{}` = `72b05c2e9ee053eb194ff8320aefe8af009bcec9`，contained by origin/main
- Release workflow `30708101913` success；非 draft/prerelease

## 2. 线上资产

六项：`aiah_0.1.10_linux_amd64`、`SHA256SUMS`、`LICENSE`、`NOTICE`、
`THIRD_PARTY_LICENSES.txt`、`THIRD_PARTY_DEPENDENCIES.md`。checksum 通过。

```text
version 0.1.10
commit  72b05c2e9ee0
SHA256  c3c841072ef19d1819cdafa4b86bcdc46becb5474f112485c08845df44cfa1e3
```

## 3. v0.1.9 → v0.1.10 严格升级

v0.1.9 程序 `update --check` 返回 `update-available`，`upgradeCommand` 逐字等于：

```bash
curl -fsSL https://raw.githubusercontent.com/dff652/ai-asset-hub/v0.1.10/scripts/install.sh | AIAH_VERSION=0.1.10 sh
```

隔离目录执行后版本/commit/SHA256 与正式资产一致；同版本复装幂等；无 stage 残留。

## 4. 安装包 CLI / TUI / MCP

- CLI `readiness`：missing / mismatch / recorded+passed 三状态正确；无私有路径与完整 reference 泄露
- TUI：PTY 启动首页出现「换机与备份」；二进制含中英文 catalog 文案
- MCP：tools/list 8 工具含 `aiah_migration_readiness`；tools/call 返回
  `kind=migration-readiness`；workspace/home 零写入

证据目录：实现者 scratch `pkg-accept/`（goal 会话私有，非仓库）。

## 5. 范围

N10.1–N10.3 已发布。N10.4、后台同步、云/NAS/Git 客户端、MCP 写操作均未做。
