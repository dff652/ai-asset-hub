# v0.1.6 bridge 发布与验收检查点

- 日期：2026-07-30
- 上一公开版本：`v0.1.5`，`main@8ca70f0cde4b`
- dev 基线：`dev@e7d813ec00e3`
- 发布版本：`v0.1.6`
- 发布提交：`main@46e6efccc9ba`
- 结论：**bridge Release、线上资产、旧版 legacy no-op、显式版本升级、幂等复装
  和正式 TUI dogfood 均已验收；当前发布后分支正在把默认 pin 与用户文档收口到
  `v0.1.6`**
- 关联：[PR #20](https://github.com/dff652/ai-asset-hub/pull/20)、
  [dev CI 30518125897](https://github.com/dff652/ai-asset-hub/actions/runs/30518125897)、
  [PR #21](https://github.com/dff652/ai-asset-hub/pull/21)、
  [main CI 30521118041](https://github.com/dff652/ai-asset-hub/actions/runs/30521118041)、
  [Release workflow 30521763546](https://github.com/dff652/ai-asset-hub/actions/runs/30521763546)、
  [v0.1.6 Release](https://github.com/dff652/ai-asset-hub/releases/tag/v0.1.6)

## 1. 为什么这是 bridge release

`v0.1.5` 已发布二进制不能追溯修复。它在 `v0.1.6` 成为 latest Release 后仍会生成
不含 `AIAH_VERSION` 的 legacy 命令：

```text
curl -fsSL https://raw.githubusercontent.com/dff652/ai-asset-hub/v0.1.6/scripts/install.sh | sh
```

按 staged-pin 规则，`v0.1.6` tag 内安装器默认版本必须继续指向已经验收的
`v0.1.5`。因此复制上面的旧命令会保持在 `v0.1.5`；这不是新修复失效，而是旧二进制
和不可变 tag 共同形成的一次性兼容边界。

本次发布必须：

1. 逐字记录 `v0.1.5` 实际生成的 legacy 命令；
2. 在隔离目录执行并确认它保持 `v0.1.5`；
3. 在 Release 说明中公开该边界和显式版本 workaround；
4. 使用 `AIAH_VERSION=0.1.6` 完成真实升级；
5. 保留 installer 默认 pin 为 `v0.1.5`，直到 `v0.1.6` 发布后验收完成。

`v0.1.6` 二进制已经包含修复后的命令生成逻辑。只有将来
`v0.1.6 → v0.1.7` 时，才能首次要求程序实际输出：

```text
.../v0.1.7/scripts/install.sh | AIAH_VERSION=0.1.7 sh
```

并把“推荐命令端到端闭环”标记为通过。

## 2. 候选范围

相对 `main@8ca70f0`，`dev@e7d813e` 的 tree delta 为 33 个文件、
964 行新增、201 行删除：

- `internal/update`：升级命令同时绑定 tag URL 和 `AIAH_VERSION`，增加精确字符串
  回归；
- `internal/tui`：窄屏升级命令拆成四行可复制文本，普通维护入口统一提示裸
  `aiah`；
- `scripts/install.sh` / `scripts/test-install.sh`：默认 pin 从已验收的
  `v0.1.4` 收口到已验收的 `v0.1.5`，测试精确拦截旧 pin；
- README / 上手指南 / CLI 文档：统一主入口、升级边界与六类使用流程；
- 四张项目原生 SVG、视觉 SOP 和静态门禁：统一字体、尺寸、对齐、版本证据与
  900px/360px 验收规则。

没有修改 `go.mod`、`go.sum`、GitHub Actions、LICENSE 或 NOTICE，也没有新增运行时
依赖。TUI E1/E2/E3.1 的业务能力已经随 `v0.1.5` 发布；本候选不把未实现的 E3.2、
MCP 状态补齐或设置/i18n 混进 bridge release。

## 3. 已有证据

### 3.1 dev 合并与 CI

- PR #20 以 squash merge 合入 `dev@e7d813e`；
- PR 的 push / pull_request 两组检查共 18 项全部通过；
- 合并后的独立 dev push CI `30518125897` 再次通过 Go、race、vet、gofmt、
  fake-HOME 闭环、lint、install-linux 和 6 个交叉编译 job；
- 本地 `dev` 与 `origin/dev` 对齐，随后从该提交建立 `release/v0.1.6`；
- `main@8ca70f0` 的 tree 与 PR #20 前 `dev@88094cd` 完全相同，但 squash 历史
  不同；候选用 `79f5c2c` 的 tree-preserving merge 记录 main 父提交，未改动任何
  文件内容，也未使用 force-push。

### 3.2 本地功能与安装器回归

在 `dev@e7d813e` 上通过：

```bash
go test ./internal/update ./internal/tui ./cmd/aiah
bash -n scripts/install.sh scripts/test-install.sh scripts/release-build.sh
./scripts/test-install.sh
./scripts/check-local.sh
```

完整门禁覆盖 dev-doctor、shell 语法和夹具、许可清单、Release checksum、
README/SVG 资产检查、Go test/race/vet/gofmt/lint，以及假 HOME
build → diff → apply → doctor → rollback 闭环。

此前变异验证已经确认：

- 从生成命令删除 `AIAH_VERSION` 时精确字符串测试会失败；
- 把 installer 默认 pin 放回 `0.1.4` 时安装器测试会失败；
- 把 hero 主命令放回 `aiah ui` 时 README 资产门禁会失败。

### 3.3 本地 Release 产物预演

使用显式临时输出目录运行：

```bash
VERSION=0.1.6 OUT=/tmp/aiah-v0.1.6-candidate.<id>/release \
  ./scripts/release-build.sh
```

结果：

- 只生成一个 `aiah_0.1.6_linux_amd64`；
- 文件为 stripped、静态 ELF x86-64；
- 二进制报告 `aiah 0.1.6, commit e7d813ec00e3`；
- LICENSE、NOTICE、两份第三方许可材料与 `SHA256SUMS` 齐全；
- 项目校验脚本和在产物目录执行的 `sha256sum --check SHA256SUMS` 全部通过。

这只是文档修改前 `dev@e7d813e` 的本地产物预演。候选文档提交后 commit 会变化，
main 提升前必须从最终候选 commit 重跑，不能把本节 SHA 或 commit 当成线上产物。

## 4. 发布门槛与结果

1. ✅ 完成本文档、roadmap 与发布说明的状态修订，并运行完整
   `./scripts/check-local.sh`；
2. ✅ 提交候选文档，push `release/v0.1.6`，创建
   `release/v0.1.6 → main` [PR #21](https://github.com/dff652/ai-asset-hub/pull/21)；
3. ✅ main 最终提交 `46e6efccc9ba` 的 9 个 CI job 全绿；
4. ✅ 从最终 main commit 重新执行 `VERSION=0.1.6 ./scripts/release-build.sh`；
5. ✅ 在所有者明确授权后创建并推送 annotated tag `v0.1.6`，peeled commit
   精确等于 `46e6efccc9ba8ee805fde317adeac65f9e063997`；
6. ✅ Release workflow 完成，下载线上产物复验 SHA256、ELF、版本、commit 与许可；
7. ✅ 按[安装、升级与 TUI dogfood SOP](../runbooks/install-upgrade-dogfood.md)完成
   `v0.1.5 → v0.1.6` bridge 验收、正式 TUI 检查和同版本幂等复装；
8. 🚧 独立发布后 [PR #22](https://github.com/dff652/ai-asset-hub/pull/22) 把
   installer 默认 pin、README、上手指南和版本证明板更新到 `v0.1.6`；合并前仍以
   PR 状态表述，不提前声称默认分支已经收口。

### 4.1 线上产物

- Release 为正式版，非 draft、非 prerelease；
- 共 6 项资产：一个 `aiah_0.1.6_linux_amd64`、`LICENSE`、`NOTICE`、
  `SHA256SUMS` 和两份第三方材料；
- `SHA256SUMS` 全部通过；二进制为 stripped、静态 ELF x86-64，大小
  `10436770` bytes；
- 二进制报告 `aiah 0.1.6, commit 46e6efccc9ba`；
- 线上二进制 SHA256：
  `2535ed343e6e398f88456f9280a1f51717b3f2a7adb1ff6f3e23f789456aef70`。

### 4.2 bridge 升级

- 隔离安装的 `v0.1.5` 报告 commit `8ca70f0cde4b`，升级前 SHA256 为
  `6f588527796a5f1a26f08488bfa1d6841716861653a20f093933280a97209c6a`；
- 旧二进制实际输出 legacy 命令，逐字等于不含 `AIAH_VERSION` 的预期模板；
- 在第二个隔离目录执行该固定命令后，版本和 SHA256 均保持 `v0.1.5`，无 stage
  残留；
- 显式设置 `AIAH_VERSION=0.1.6` 后升级到 commit `46e6efccc9ba`，SHA256 变为
  线上值，mode `0755`，无 stage 残留；
- 同版本复装保持相同 SHA256，安装器报告 already installed；
- Release 说明已公开 legacy 边界与显式版本 workaround。

这证明 bridge 行为和 workaround 正确，但**不等于修复后推荐命令已经完成
Release → Release 闭环**。该证明必须等 `v0.1.6 →` 下一版本时，由 `v0.1.6`
二进制实际输出并执行带 `AIAH_VERSION` 的命令。

### 4.3 正式 TUI

- 裸 `aiah` 进入任务首页；显式隔离入口显示 5 项资产和健康安装；
- 版本页显示 `0.1.6`、12 位 commit 和 build date，打开页面不联网，按 `c` 后显示
  `current/latest 0.1.6`；
- Doctor 显示 deployment、一个 backup、5 项 unchanged、0 drift；
- typed `rollback` 移除 5 个目标文件；CLI 对账为 `candidateAssets=0`、
  `deployment=false`，backup 与 deployment 审计记录保留；
- 全部写入只发生在 `/tmp/aiah-upgrade-v016.*`，临时目录验收后已清理；真实
  `~/.local/bin/aiah` 未被该流程替换。

## 5. 最终判断

没有发现阻止 `v0.1.6` 使用的新 P0/P1 发布问题。准确状态是：

> **`v0.1.6` 已正式发布并完成 Linux amd64 线上产物、显式 bridge 升级、legacy
> no-op、幂等复装与正式 TUI 验收；旧版推荐命令限制已在 Release 说明公开。**

发布后只剩默认 pin 与用户文档收口。该独立 PR 通过后，产品开发主线进入 E3.2
跨设备连续向导；不要把新写流程混入本次发布记录提交。
