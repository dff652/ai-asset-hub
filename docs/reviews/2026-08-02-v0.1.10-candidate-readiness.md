# v0.1.10 发布候选就绪检查点

- 日期：2026-08-02
- 发布基线：`main@feaf95a859d8`
- 开发来源：`dev@f3017feb9525`
- 候选分支：`release/v0.1.10`
- 状态：候选文件树与证据已准备；候选 PR、main CI、tag、Release 与正式安装包验收待后续步骤

## 1. 候选目的与范围

`v0.1.10` 正式交付 N10 迁移准备检查（不含 N10.4）：

1. N10.1：共享 `readiness` Core、schema、`aiah readiness` CLI（PR #43）
2. N10.2：TUI「换机与备份」页面与三态状态展示（PR #44）
3. N10.3：只读 MCP 工具 `aiah_migration_readiness`，ADR-0005 7→8 工具（PR #46）

明确不包含：N10.4 自动证据记录器、后台同步、云/NAS/Git 客户端、MCP 写操作。

安装器默认 pin 按 staged-pin 规则保持已验收的 `v0.1.9`；只有正式包验收通过后
才另开 PR 更新 pin 与用户文档。

## 2. 验证

- 真资产库临时副本三状态（missing/mismatch/recorded）通过，live 库未改
- N10.2/N10.3 新安全/行为测试均完成变异验证
- 合入 `dev` 后完整 `./scripts/check-local.sh` 与本地 `VERSION=0.1.10` release-build 通过
- dev CI：N10.1 #43 / N10.2 #44 / N10.3 #46 均在精确 head SHA 上成功

## 3. 出口门禁

1. 候选生产代码 tree 与 `origin/dev@f3017fe` 一致（允许本候选检查点文档差异）
2. 候选 PR 到 main 的 CI 全绿并合入
3. 合并后 main CI `headSha` = `origin/main` 且 success
4. annotated tag `v0.1.10^{}` = 该 main SHA 且 contained by origin/main
5. Release 六项资产 + SHA256 + version/commit 自检
6. 严格 v0.1.9→v0.1.10 推荐命令升级 + 安装包 CLI/TUI/MCP 验收
7. 独立 pin/docs PR 后 `git diff origin/main origin/dev` 空树
