# v0.1.11 发布候选就绪检查点

- 日期：2026-08-02
- 目的：首个**带 macOS 产物**的公开 Release（完整支持 linux/amd64 + darwin/arm64；
  darwin/amd64 交叉编译+冒烟）
- 代码树：与 `origin/dev@1715e70` / `origin/main` 同步后的多平台发布树
- 默认 installer pin：**保持 `v0.1.10`**（staged-pin），验收通过后再独立 PR

## 范围

- `scripts/_release_platforms.sh` 三平台清单；`release-build` / 校验器共用
- 安装器 uname 映射 + SUMS 前置检查；linux/arm64 显式拒绝
- CI `macos` 真机全套（与 linux 同内容）；**分支保护必需检查仍由所有者配置**
- 文档三档支持 + Gatekeeper 说明
- 测试路径拼写与 release 形状校验的变异可测夹具

## 非范围

- Apple 签名/公证；Windows / linux/arm64 发布；N10.4；MCP 写；改写 v0.1.10 tag

## 出口

1. main 精确 head CI success
2. annotated `v0.1.11^{}` = 该 main SHA 且 contained by origin/main
3. Release 三二进制 + 许可材料 + SHA256SUMS；形状与 checksum 通过
4. v0.1.10 → v0.1.11 严格升级契约；旧 pin 无 darwin 时失败信息明确
5. pin/docs 独立 PR 后 main/dev 树一致
