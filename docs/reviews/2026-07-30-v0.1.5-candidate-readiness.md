# v0.1.5 候选就绪检查点

- 日期：2026-07-30
- 候选：`dev@8552bef5b332264576dc221b0ec87c71dc6eb16a`
- 计划版本：`v0.1.5`
- 结论：**dev 候选就绪，可以进入 main 提升评审；尚未 tag 或发布**
- 关联：[PR #16](https://github.com/dff652/ai-asset-hub/pull/16)、
  [dev CI 30513978913](https://github.com/dff652/ai-asset-hub/actions/runs/30513978913)

## 1. dev 收口

受保护的 `dev` 不接受直接 push。本轮通过 PR #16 合入，并用 rebase merge 保留两个
可独立审阅的提交：

- `ba40896`：任务首页、统一资产状态、纳入/更新/移出、连续应用向导、E3.1 只读
  迁移状态和安全加固；
- `8552bef`：README mode、三张项目原生静态 SVG、重构前快照和严格复审记录。

PR 的 push 与 pull_request 两组 CI 全绿；合并后的 `dev@8552bef` 又触发独立 push
CI `30513978913`，9 个 job 全绿。仓库本地 `dev` 与 `origin/dev` 对齐，工作树干净。

## 2. 候选安装 dogfood

所有写入均位于隔离的 `/tmp/aiah-upgrade.*`，没有覆盖真实
`~/.local/bin/aiah`，也没有把用户资产应用到真实 HOME。

候选使用 `0.1.5-dev.n1` 开发标记：

- 从 PR 头提交构建并原子替换到隔离安装目录，stage 自检、mode `0755`、内容比较
  和残留 stage 检查通过；
- 裸 `aiah` 在真实 TTY 中进入任务首页，自动显示资产扫描和当前安装状态，`q`
  干净退出；
- TUI 完成“未纳管 → typed 纳入 → 自动 profile/build → 变更预览 → typed
  `apply` → 成功摘要”；
- Doctor 显示 2 个文件无漂移、当前包/版本/恢复点正确；typed `rollback` 后恢复
  1、移除 1，当前 deployment 清空；
- 修改隔离源端后显示“待更新”，typed `update` 后资产库正文与源端 SHA256 一致；
- typed `remove` 后 manifest 仍可验证、资产库正文删除、源端文件保留；
- E3.1 在未安装状态下读取用户显式选择的隔离通道，显示一个 `0.1.0/personal`
  发布、SHA256 和“与资产库版本一致”，没有在页面执行 publish/pull/apply；
- 同一候选重复原子替换前后 SHA256 一致，无 stage 残留。

PR 头与 rebase merge 后 `dev@8552bef` 的 Git tree 完全一致。随后从合并提交重新构建
候选，版本报告精确指向 `8552bef5b332`，并再次通过隔离原子替换与裸 `aiah` 真 TTY
启动/退出。

## 3. 本地 Release 产物预演

运行：

```bash
VERSION=0.1.5 ./scripts/release-build.sh
./scripts/check-release-checksums.sh
```

结果：

- 只生成 `aiah_0.1.5_linux_amd64`，没有把交叉编译目标误放进 Release；
- 产物为静态 ELF x86-64；
- 二进制报告 `aiah 0.1.5, commit 8552bef5b332`；
- `SHA256SUMS` 全部通过，二进制 SHA256 为
  `03cf787e1c43ed0295929c9617559a48b48b204bfa909e1444263e5e6a9f86fe`；
- LICENSE、NOTICE 和两份第三方许可材料齐全。

## 4. 仍需完成的发布门槛

当前证据只把候选推进到 **dev-ready**，不等于已发布或安装器升级已通过：

1. 用 PR 把同一 dev tree 提升到 `main`，等待 main CI 全绿；
2. 再次明确确认后创建 annotated tag `v0.1.5`；tag 必须精确指向通过 CI 的 main
   commit；
3. 等 Release workflow 全绿，下载线上产物并复验 SHA256、ELF、版本和 commit；
4. 用公开 `v0.1.4` 通过真实安装器升级到 `v0.1.5`，复验裸 `aiah`、统一状态、
   E3.1、Doctor/rollback 和同版本幂等复装；
5. 只有以上通过后，另开 PR 更新 `scripts/install.sh`、README 和上手指南的默认
   pin。

因此不得把本文件的候选替换写成“`v0.1.4 → v0.1.5` 安装器升级已通过”。
